'use strict';

const binarySearch = require('../array-binary-search.js');

describe('binary search function', () => {
  it('should return -1 for empty arrays', () => {});

  it('should return -1 when a number is not found', () => {});

  it('should return the index of the number in the array, if it is found', () => {});

  describe('should be able to handle non-numerical search values', () => {
    it('with numerical array values', () => {});

    it('and non-numberical serach values', () => {});
  });

  describe('should be able to handle non-numerical array values', () => {
    it('with numerical array values', () => {});

    it('and with non-numerical search values', () => {});
  });
});